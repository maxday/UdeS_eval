<h1>Liste des questions</h1>

<div class="btn-toolbar">
  <%= link_to 'Nouvelle question', new_question_path, :class => "btn btn-primary" %>
  <%= link_to 'Retour', home_path, :class => "btn btn-inverse" %>
</div>

<table class="table table-striped">
  <tr>
    <th>Nom</th>
    <th>Catégorie</th>
    <th>Set</th>
    <th></th>
    <th></th>
  </tr>

<% @questions.each do |question| %>
  <% c = Category.find(question.category_id) %>
  <tr>
    <td><%= question.name %></td>
    <td><%= c.name %></td>
    <td><%= Questionset.find(c.questionset_id).name %></td>
    <td><%= link_to 'Editer', edit_question_path(question), :class => "btn btn-info" %></td>
    <td><%= link_to 'Supprimer', question, method: :delete, data: { confirm: 'Etes-vous sur ?' }, :class => "btn btn-danger" %></td>
  </tr>
<% end %>
</table>
